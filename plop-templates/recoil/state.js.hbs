import { atom, selector } from 'recoil'

export const {{camelCase name}}State = atom<number>({
    key: '{{camelCase name}}',
    default: 0
});
  
export const {{camelCase name}}StrState = selector<string>({
    key: '{{camelCase name}}Str',
    get: ({get}) => `{{camelCase name}}State: ${get({{camelCase name}}State)}`
});