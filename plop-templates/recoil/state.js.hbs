import { atom, selector } from 'recoil'

export const {{camelCase name}}State = atom({
  key: '{{camelCase name}}',
  default: 0
});
  
export const {{camelCase name}}StrState = selector({
  key: '{{camelCase name}}Str',
  get: ({get}) => `{{camelCase name}}State: ${get({{camelCase name}}State)}`
});